/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'avatars.githubusercontent.com',
            'm.media-amazon.com',
            'github.com',
        ],
    },
}

module.exports = nextConfig
